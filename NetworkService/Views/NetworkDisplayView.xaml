<UserControl x:Class="NetworkService.Views.NetworkDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:model="clr-namespace:NetworkService.Model"
             xmlns:root="clr-namespace:NetworkService"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBg"    Color="#FF1A1A2E"/>
        <SolidColorBrush x:Key="SecondaryBg"  Color="#FF16213E"/>
        <SolidColorBrush x:Key="CardBg"       Color="#FF0F3460"/>
        <SolidColorBrush x:Key="AccentGreen"  Color="#FF22C55E"/>
        <SolidColorBrush x:Key="AccentYellow" Color="#FFEAB308"/>
        <SolidColorBrush x:Key="AccentRed"    Color="#FFEF4444"/>
        <SolidColorBrush x:Key="TextPrimary"  Color="#FFE2E8F0"/>
        <SolidColorBrush x:Key="TextMuted"    Color="#FF94A3B8"/>
        <SolidColorBrush x:Key="BorderColor"  Color="#FF334155"/>
        <SolidColorBrush x:Key="CanvasEmpty"  Color="#FF1E2D45"/>

        <LinearGradientBrush x:Key="HeaderGrad" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#FF16A34A" Offset="0"/>
            <GradientStop Color="#FF15803D" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource AccentGreen}"/>
            <Setter Property="Foreground"      Value="#FF0F172A"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Padding"         Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF16A34A"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{StaticResource BorderColor}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF1E4080"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{StaticResource BorderColor}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClearBtn" TargetType="Button" BasedOn="{StaticResource SecondaryBtn}">
            <Setter Property="Width"   Value="55"/>
            <Setter Property="Height"  Value="22"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>

        <Style x:Key="CanvasStyle" TargetType="Canvas">
            <Setter Property="Background" Value="{StaticResource CanvasEmpty}"/>
            <Setter Property="Margin"     Value="4"/>
        </Style>

        <Style x:Key="TreeStyle" TargetType="TreeView">
            <Setter Property="Background"   Value="{StaticResource SecondaryBg}"/>
            <Setter Property="Foreground"   Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush"  Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize"     Value="13"/>
            <Setter Property="Padding"      Value="4"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource PrimaryBg}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ═══ LEVI PANEL ═══ -->
        <Border Grid.Column="0" Background="{StaticResource SecondaryBg}" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- TreeView grupisano po tipu -->
                <Border Grid.Row="0" Background="{StaticResource CardBg}" CornerRadius="6" Margin="0,0,0,8">
                    <StackPanel>
                        <Border Background="{StaticResource HeaderGrad}" CornerRadius="6,6,0,0" Padding="10">
                            <TextBlock Text="🌿 DER Resources" FontSize="14" FontWeight="Bold"
                                       Foreground="White" TextAlignment="Center"/>
                        </Border>
                        <TreeView Name="ResourceTreeView"
                                  ItemsSource="{Binding AllResources}"
                                  Style="{StaticResource TreeStyle}"
                                  Margin="6" Height="280">
                            <TreeView.Resources>
                                <!-- Grupni header -->
                                <HierarchicalDataTemplate DataType="{x:Type root:DerResourcesByType}"
                                                          ItemsSource="{Binding Resources}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding TypeName}" FontWeight="Bold"
                                                   Foreground="{StaticResource AccentYellow}"/>
                                        <TextBlock Text=" (" Foreground="{StaticResource TextMuted}"/>
                                        <TextBlock Text="{Binding Resources.Count}" Foreground="{StaticResource TextMuted}"/>
                                        <TextBlock Text=")" Foreground="{StaticResource TextMuted}"/>
                                    </StackPanel>
                                </HierarchicalDataTemplate>

                                <!-- Resurs stavka -->
                                <DataTemplate DataType="{x:Type model:DerResource}">
                                    <StackPanel Orientation="Horizontal" Cursor="Hand">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                                <i:InvokeCommandAction
                                                    Command="{Binding DataContext.MouseDownCommand,
                                                              RelativeSource={RelativeSource AncestorType=TreeView}}"
                                                    CommandParameter="{Binding}"/>
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="MouseMove">
                                                <i:InvokeCommandAction
                                                    Command="{Binding DataContext.MouseMoveCommand,
                                                              RelativeSource={RelativeSource AncestorType=TreeView}}"
                                                    CommandParameter="{Binding}"/>
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                                                <i:InvokeCommandAction
                                                    Command="{Binding DataContext.ResetDragCommand,
                                                              RelativeSource={RelativeSource AncestorType=TreeView}}"
                                                    CommandParameter="{Binding}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                        <Image Source="{Binding ResourceType.ImagePath}"
                                               Width="20" Height="20" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Id}" Foreground="{StaticResource TextMuted}"
                                                   Margin="0,0,4,0"/>
                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextPrimary}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </TreeView.Resources>
                        </TreeView>
                    </StackPanel>
                </Border>

                <!-- Undo dugmad -->
                <StackPanel Grid.Row="1" Margin="0,0,0,8">
                    <Button Content="↩ Undo Last" Command="{Binding UndoCommand}"
                            Style="{StaticResource SecondaryBtn}" HorizontalAlignment="Stretch"
                            Margin="0,0,0,6"
                            ToolTip="Undo the last performed action"/>
                    <Button Content="↩↩ Undo All" Command="{Binding UndoAllCommand}"
                            Style="{StaticResource SecondaryBtn}" HorizontalAlignment="Stretch"
                            ToolTip="Undo ALL performed actions (CG4)"/>
                </StackPanel>

                <!-- CG4 History paleta -->
                <Border Grid.Row="2" Background="{StaticResource CardBg}" CornerRadius="6" Padding="10"
                        Margin="0,0,0,0">
                    <StackPanel>
                        <TextBlock Text="📋 Action History" FontSize="13" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                        <ListBox ItemsSource="{Binding ActionHistory}"
                                 Background="Transparent" BorderThickness="0"
                                 Foreground="{StaticResource TextMuted}"
                                 FontSize="10" MaxHeight="140"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>

            </Grid>
        </Border>

        <!-- ═══ DESNI PANEL: Drag&Drop mreža ═══ -->
        <Border Grid.Column="1" Background="{StaticResource PrimaryBg}" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource HeaderGrad}" CornerRadius="6"
                        Padding="10" Margin="0,0,0,10">
                    <TextBlock Text="⚡ Network Display — Drag resources from tree, drop onto grid cells"
                               Foreground="White" FontSize="14" FontWeight="Bold"/>
                </Border>

                <!-- 4×3 mreža = 12 canvas-ova -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Macro: za svaki od 12 canvas-ova isti pattern -->

                    <!-- Row 0 -->
                    <DockPanel Grid.Row="0" Grid.Column="0" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom"
                                HorizontalAlignment="Center" Margin="0,2,0,0"
                                Command="{Binding ClearCanvasCommand}"
                                CommandParameter="{Binding ElementName=Canvas1}"
                                ToolTip="Return this resource to the tree view"/>
                        <Canvas x:Name="Canvas1" Tag="1" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded">
                                    <i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}"
                                                           CommandParameter="{Binding ElementName=Canvas1}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Drop">
                                    <i:InvokeCommandAction Command="{Binding DropCommand}"
                                                           CommandParameter="{Binding ElementName=Canvas1}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}"
                                                           CommandParameter="{Binding ElementName=Canvas1}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove">
                                    <i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}"
                                                           CommandParameter="{Binding ElementName=Canvas1}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold"
                                       Foreground="White" TextWrapping="Wrap"
                                       Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="0" Grid.Column="1" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom"
                                HorizontalAlignment="Center" Margin="0,2,0,0"
                                Command="{Binding ClearCanvasCommand}"
                                CommandParameter="{Binding ElementName=Canvas2}"
                                ToolTip="Return this resource to the tree view"/>
                        <Canvas x:Name="Canvas2" Tag="2" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded">
                                    <i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}"
                                                           CommandParameter="{Binding ElementName=Canvas2}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Drop">
                                    <i:InvokeCommandAction Command="{Binding DropCommand}"
                                                           CommandParameter="{Binding ElementName=Canvas2}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}"
                                                           CommandParameter="{Binding ElementName=Canvas2}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove">
                                    <i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}"
                                                           CommandParameter="{Binding ElementName=Canvas2}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold"
                                       Foreground="White" TextWrapping="Wrap"
                                       Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="0" Grid.Column="2" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom"
                                HorizontalAlignment="Center" Margin="0,2,0,0"
                                Command="{Binding ClearCanvasCommand}"
                                CommandParameter="{Binding ElementName=Canvas3}"/>
                        <Canvas x:Name="Canvas3" Tag="3" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas3}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas3}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas3}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas3}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="0" Grid.Column="3" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas4}"/>
                        <Canvas x:Name="Canvas4" Tag="4" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas4}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas4}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas4}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas4}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <!-- Row 1 -->
                    <DockPanel Grid.Row="1" Grid.Column="0" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas5}"/>
                        <Canvas x:Name="Canvas5" Tag="5" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas5}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas5}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas5}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas5}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="1" Grid.Column="1" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas6}"/>
                        <Canvas x:Name="Canvas6" Tag="6" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas6}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas6}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas6}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas6}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="1" Grid.Column="2" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas7}"/>
                        <Canvas x:Name="Canvas7" Tag="7" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas7}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas7}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas7}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas7}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="1" Grid.Column="3" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas8}"/>
                        <Canvas x:Name="Canvas8" Tag="8" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas8}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas8}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas8}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas8}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <!-- Row 2 -->
                    <DockPanel Grid.Row="2" Grid.Column="0" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas9}"/>
                        <Canvas x:Name="Canvas9" Tag="9" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas9}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas9}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas9}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas9}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="2" Grid.Column="1" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas10}"/>
                        <Canvas x:Name="Canvas10" Tag="10" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas10}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas10}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas10}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas10}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="2" Grid.Column="2" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas11}"/>
                        <Canvas x:Name="Canvas11" Tag="11" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas11}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas11}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas11}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas11}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <DockPanel Grid.Row="2" Grid.Column="3" LastChildFill="True">
                        <Button Content="Clear" Style="{StaticResource ClearBtn}" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,2,0,0" Command="{Binding ClearCanvasCommand}" CommandParameter="{Binding ElementName=Canvas12}"/>
                        <Canvas x:Name="Canvas12" Tag="12" Style="{StaticResource CanvasStyle}" AllowDrop="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded"><i:InvokeCommandAction Command="{Binding CanvasLoadedCommand}" CommandParameter="{Binding ElementName=Canvas12}"/></i:EventTrigger>
                                <i:EventTrigger EventName="Drop"><i:InvokeCommandAction Command="{Binding DropCommand}" CommandParameter="{Binding ElementName=Canvas12}"/></i:EventTrigger>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown"><i:InvokeCommandAction Command="{Binding MouseDownCanvasCommand}" CommandParameter="{Binding ElementName=Canvas12}"/></i:EventTrigger>
                                <i:EventTrigger EventName="MouseMove"><i:InvokeCommandAction Command="{Binding MouseMoveCanvasCommand}" CommandParameter="{Binding ElementName=Canvas12}"/></i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TextBlock Text="" FontSize="10" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Width="90" TextAlignment="Center" Margin="5,4,0,0"/>
                        </Canvas>
                    </DockPanel>

                    <!-- Transparentni canvas za iscrtavanje linija veza (iznad svega) -->
                    <Canvas x:Name="LineCanvas"
                            IsHitTestVisible="False"
                            Grid.Row="0" Grid.Column="0"
                            Grid.ColumnSpan="4" Grid.RowSpan="3">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Loaded">
                                <i:InvokeCommandAction Command="{Binding SetLineCanvasCommand}"
                                                       CommandParameter="{Binding ElementName=LineCanvas}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <ItemsControl ItemsSource="{Binding Lines}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate><Canvas/></ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Line X1="{Binding X1}" Y1="{Binding Y1}"
                                          X2="{Binding X2}" Y2="{Binding Y2}"
                                          Stroke="{Binding Stroke}"
                                          StrokeThickness="{Binding StrokeThickness}"
                                          StrokeDashArray="{Binding StrokeDashArray}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Canvas>

                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
